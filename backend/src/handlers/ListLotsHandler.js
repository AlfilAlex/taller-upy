import { ListLotsFn } from '../controllers/ListLotsFn.js';

export const ListLotHandler = async (event) => {
    const { status, createdDay } = event.queryStringParameters || {};
    // CONTRUYE EL RESTO DE LA FUNCIÓN
}